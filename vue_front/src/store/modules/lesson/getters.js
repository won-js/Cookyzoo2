const lessonId = state => state.lessonId;
const price = state => state.price;
const thumbnail = state => state.thumbnail;
const categoryId = state => state.categoryId;

export default {
	lessonId,
	price,
	thumbnail,
	categoryId,
};
